/**
 * Scene mount and lifecycle primitives for the Reactylon adapter.
 *
 * @module mount
 * @category Player Experience
 */

export { SceneRoot, type SceneRootProps } from './SceneRoot.js';
export { MAX_FRAME_DELTA_SECONDS, useBeforeRender } from './useBeforeRender.js';

# Reject unsupported Node versions while allowing every maintained release from
# the package.json floor through the latest stable runtime. Maintainer tool
# versions are selected once in mise.toml, not duplicated here or in CI.
engine-strict=true
